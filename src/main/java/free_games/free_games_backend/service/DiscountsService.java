package free_games.free_games_backend.service;

/*
import free_games.free_games_backend.dto.DiscountDto;
import free_games.free_games_backend.repository.DiscountRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
@RequiredArgsConstructor
public class DiscountsService {
    private final DiscountRepository discountRepository;

    @Transactional(readOnly = true)
    public List<DiscountDto> getPage(Integer limit, Integer offset) {
        var entityPage = discountRepository.findAll();

    }


}
        */